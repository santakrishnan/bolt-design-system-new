# Bolt Design System - Environment Variables

# Development server port
# Next.js will use this as the preferred port
# If busy, it will automatically try the next available port (4001, 4002, etc.)
PORT=4000

# Next.js public URL
NEXT_PUBLIC_APP_URL=http://localhost:4000

# Registry URL for component registry
REGISTRY_URL=http://localhost:4000/r

# Node.js SSL/TLS Certificate Validation
# Set to '0' to disable SSL certificate validation (useful for development with self-signed certificates)
# WARNING: Only use in development! Never disable in production!
# NODE_TLS_REJECT_UNAUTHORIZED=0

# Turbopack System TLS Certificates
# Enable system TLS certificates for Turbopack (fixes Google Fonts SSL errors)
# Recommended when behind corporate proxy or with SSL inspection
# NEXT_TURBOPACK_EXPERIMENTAL_USE_SYSTEM_TLS_CERTS=1
